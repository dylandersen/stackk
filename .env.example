# Encryption Key for Vercel API Token Encryption
# This is a 64-character hex string (32 bytes) used for AES-256-GCM encryption
# Generate a new one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Create a .env.local file and set your own unique ENCRYPTION_KEY
ENCRYPTION_KEY=your-64-character-hex-encryption-key-here

# InstantDB Configuration (if not already set)
# NEXT_PUBLIC_INSTANT_APP_ID=your-app-id
# INSTANT_APP_ADMIN_TOKEN=your-admin-token
